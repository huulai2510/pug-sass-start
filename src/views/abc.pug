extends ../templates/layout

block body
  .flex.flex-col.flex-fill.justify-content-center.align-items-center
    h1(style="font-size: 5em; font-weight: 400"): code &lt;Template /&gt;
    h1 casd
  div
    h1 lai
